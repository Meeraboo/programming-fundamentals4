#include <iostream>
using namespace std;
int main()
{
    int n;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cout << "Enter how many numbers u want to enter: ";
        cin >> n;
        if (arr[i] = arr[i] + 1)
        {
            cout << " number already exist.";
        }
    }
}